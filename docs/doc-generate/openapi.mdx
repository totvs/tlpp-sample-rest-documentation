---
title: OpenAPI gerado
sidebar_position: 4
displayed_sidebar: restSidebar
---

import {getExternalRepo, githubFileUrl, githubRawUrl} from '@site/src/data/external-repos';

export const sampleRepo = getExternalRepo('doc-generate');

# OpenAPI gerado (`api_doc_8080.yaml`)

Snapshot em [`output/api_doc_8080.yaml`]({githubFileUrl(sampleRepo, 'output/api_doc_8080.yaml')}) no repositório de exemplos — resultado de `tlpp.doc.generate()` aplicado aos metadados daquele projeto.

## O que contém

- Paths dos 5 exemplos básicos (`/rest/sample/doc/basic/01` … `/05/multiline`)
- Path completo `PUT /rest/sample/doc/complete/{param1}`
- Endpoints dinâmicos `/init/dyn`, `/class/{param1}`
- `components/schemas` de `sampleBodyIn`, `sampleBodyOutOk`, `sampleBodyOutError`

## Explorar no site

[Explorador OpenAPI](/tools/explorador-openapi) — cole ou carregue o YAML e navegue pelos endpoints (sem chamar API real).

[YAML no GitHub (raw)]({githubRawUrl(sampleRepo, 'output/api_doc_8080.yaml')})

## Relação metadado → YAML

Cada página em [Exemplos](/docs/tlpp/rest/metadados/exemplos/basic/description-only) mostra o trecho TLPP e o trecho correspondente do YAML via componente `CodeToOpenAPI`.
