---
title: "01 — description (texto)"
sidebar_label: "01 · description"
sidebar_position: 1
displayed_sidebar: restSidebar
---

import CodeToOpenAPI from '@site/src/components/CodeToOpenAPI';

# Exemplo 01 — `description` como texto

**Arquivo:** `src/rest/01_traditional_annotation/basic_description.tlpp`  
**Metadado demonstrado:** `description` com string literal (sem `title`, `params`, `responses`).

<CodeToOpenAPI
tlpp={`@Get(;
  endpoint="/rest/sample/doc/basic/01",;
  description="Exemplo de documentação simples, somente descrição";
)
function U_rest_sample_01_basic() as logical`}
yaml={`/rest/sample/doc/basic/01:
  get:
    description: Exemplo de documentação simples, somente descrição
    responses:
      200:
        description: Success`}
/>

**Conceito:** modo mais simples do metadado `description` — ver [description](/docs/tlpp/rest/metadados/description).

**Próximo passo:** [Annotation REST](/docs/tlpp/rest/metadados/rest-annotation) — `title`, `params` e `responses` na própria `@Get`.
